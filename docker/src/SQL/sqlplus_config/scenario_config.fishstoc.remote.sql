--define the local variables to indicate the scenario the SQL*Plus script is being run
DEFINE V_APP_LOCATION_NAME = 'Remote';
DEFINE V_DB_LOCATION_NAME = 'Remote';
DEFINE V_NETWORK_NAME = 'FishSTOC';

